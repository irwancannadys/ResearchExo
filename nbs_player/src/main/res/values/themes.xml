<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ResearchExo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ResearchExo" parent="Base.Theme.ResearchExo" />

    <style name="CustomSeekBarStyle" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressDrawable">@drawable/seekbar_progress</item>
        <item name="android:thumb">@drawable/seekbar_thumb</item>
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">0dp</item>
    </style>

    <attr name="forwardRewindButtonStyle" format="reference" />

    <!-- Theme -->
    <style name="Theme.App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Other theme attributes -->

        <!-- Default style untuk ForwardRewindButton -->
        <item name="forwardRewindButtonStyle">@style/Widget.App.ForwardRewindButton</item>
        <item name="playerSeekBarStyle">@style/Widget.App.PlayerSeekBar</item>
    </style>
</resources>